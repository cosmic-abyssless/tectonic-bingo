// Import-free — safe for the admin bundle (BingoSettingsForm's theme select
// must not pull in theme code). Keep in sync with registry.ts's `loaders`.
export const THEME_KEYS = ["default"] as const;
